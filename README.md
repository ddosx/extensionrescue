# Extension_Rescue - простая програма для восстановления расширения файла
## использование
`git clone https://github.com/ddosx/extension_rescue`
`cd extension_rescue`
`pip install fleep tqdm colorama rich`
`python3 "путь к директории для восстановления"`
